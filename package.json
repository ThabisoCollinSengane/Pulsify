{
  "name": "pulsify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Build completed' && date > build-timestamp.txt",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@sentry/node": "^7.120.0",
    "@supabase/supabase-js": "^2.43.0",
    "nodemailer": "^6.9.14",
    "web-push": "^3.6.7"
  }
}

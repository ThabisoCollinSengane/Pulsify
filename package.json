{
  "name": "pulsify",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Build completed' && date > build-timestamp.txt",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.0"
  }
}

# Guard: never deploy a root index.html — it shadows the /(.*) rewrite that
# serves apps/landing-page/index.html for "/". (See CLAUDE.md deployment gotcha.)
/index.html

# Backup files (future-proof guards — backups should never reach production)
*.bak
*.bak.*
*.backup
*.backup.*

# TikTok leads cron — excluded until Pro plan (Hobby limit: 12 serverless functions).
# To activate: upgrade to Pro, remove this line, add entry back to vercel.json functions
# and crons sections, and set RAPIDAPI_KEY in Vercel env vars.
api/cron/tiktok-leads.js

# Dev-only folders not needed for deployment
_archive/
db/
docs/
scripts/
supabase/
.claude/
.idx/

